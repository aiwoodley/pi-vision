# Raspberry Pi Vision AI Dependencies
# Tested on Raspberry Pi 4 (4GB+) with Raspberry Pi OS Bookworm (64-bit)

# Core ML/Vision
tflite-runtime>=2.14.0
numpy>=1.24.3,<2.0
Pillow>=10.1.0

# OpenCV — prefer the system package (python3-opencv) on Pi 4 for
# hardware-accelerated codecs.  Fall back to pip only if needed:
opencv-python-headless>=4.8.0

# picamera2 — installed via apt (python3-picamera2) on Pi OS Bookworm.
# Uncomment below ONLY if you need a pip-based install in a venv:
# picamera2>=0.3.17

# Utilities
psutil>=5.9.6

# For YOLOv8 support (optional, heavier — needs 4GB+ RAM)
# ultralytics>=8.0.200

# For Google Coral Edge TPU (optional)
# tflite-support>=0.4.4
